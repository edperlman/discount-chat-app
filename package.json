{
  "name": "discount-chat-app",
  "version": "1.0.0",
  "description": "A clean start for Discount Chat App Monorepo",
  "main": "app.js",
  "private": true,
  "scripts": {
    "dev": "npm run clean && npm install && npm start",
    "build": "npm run clean && npm install && npm run compile",
    "clean": "rimraf node_modules .turbo package-lock.json",
    "start": "node src/server.js",
    "test": "echo 'No tests configured yet.'",
    "setup": "npm install && npm run build",
    "format": "prettier --write \"**/*.{js,ts,json,md}\"",
    "build:services": "npm run compile",
    "compile": "echo 'Building and compiling services...' && tsc"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "mongodb": "^6.12.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "@types/react": "^18.0.26",
    "@types/react-dom": "^18.0.11",
    "prettier": "^3.1.0",
    "rimraf": "^6.0.1",
    "typescript": "^5.2.2"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/your-repository.git"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  },
  "author": "Ed Perlman",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/your-repository/issues"
  },
  "homepage": "https://github.com/your-repository#readme"
}
